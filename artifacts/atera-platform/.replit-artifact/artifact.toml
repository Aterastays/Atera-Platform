kind = "web"
previewPath = "/"
title = "Atera Platform"
version = "1.0.0"
id = "artifacts/atera-platform"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25814

[services.development]
run = "pnpm --filter @workspace/atera-platform run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/atera-platform", "run", "build" ]
publicDir = "artifacts/atera-platform/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25814"
BASE_PATH = "/"
